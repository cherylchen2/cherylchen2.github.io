on:
  push:
    branches:
      - "main"

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      runs:
        working-directory: ./cherylchen
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '22'

      - name: Install dependencies
        run: npm install

      - name: Build project
        run: npm run build

      - name: Deploy to gh pages
        run: |
          npx angular-cli-ghpages --dir=dist/cherylchen
        env:
          CI: true
          GH_TOKEN: ${{ secrets.GH_TOKEN }}